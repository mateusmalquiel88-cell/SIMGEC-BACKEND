# SIMGEC Backend Configuration

# JWT Secret (change this in production)
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=8h

# Local server port
PORT=3000

# Database path (optional, defaults to simgec.db in root)
DATABASE_PATH=./data/simgec.db

# Environment mode
NODE_ENV=production
